{% set path = path('app_game_new') %}
{% set title = 'Créez votre sessions' %}
<turbo-stream action="update" target="modal">
    <template>
        {{ include('game/_form.html.twig') }}
    </template>
</turbo-stream>