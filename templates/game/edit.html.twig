{% set path = path('app_game_edit', { game: game.id }) %}
{% set title = 'Ma Session de Jeu' %}
<turbo-stream action="update" target="modal">
    <template>
        {{ include('game/_form.html.twig') }}
    </template>
</turbo-stream>