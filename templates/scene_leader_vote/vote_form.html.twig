<turbo-stream action="update" target="modal">
    <template>
        {{ form_start(form, {attr: {class: 'form grid gap-2'}}) }}
            <h2>Votez pour la scène proposer par</h2>
            {{ form_row(form.votedForPlayer, {
                attr: {class: 'form-row'},
            }) }}
            <button class="button" type="submit">Voter</button>
        {{ form_end(form) }}
    </template>
</turbo-stream>
